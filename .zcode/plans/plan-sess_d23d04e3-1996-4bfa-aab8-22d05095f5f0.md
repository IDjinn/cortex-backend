## 1. Chat na mesma tela (estilo ChatGPT)

**Mobile (`C:\dev\react-native\Cortex`)**
- Extrair a UI de conversa de `app/conversation/[id].tsx` (header + lista + composer, linhas 44–186) para **`components/chat/ConversationView.tsx`**, com props `{ id, initial?, onExit }`. Leva junto: estado `input`, autoscroll, `pendingInitialRef` + efeito de auto-send (que já espera a conversa existir nas stores — funciona para guest e authed sem mudanças em `useChatSession`, que é agnóstico de rota).
- `app/conversation/[id].tsx` vira wrapper fino (`onExit={() => router.back()}`) — deep links continuam funcionando.
- `app/(tabs)/index.tsx`: novo estado `activeId`. `handleSend` cria a conversa e faz `setActiveId(id)` em vez de `router.push(...?initial=)` — a home troca a saudação pelo `ConversationView` in-place, com o texto pendente como `initial`. `onExit` volta para a tela de saudação. `handleSelectConversation` do Sidebar também vira `setActiveId(id)` (sem push); "nova conversa" limpa `activeId`. Sidebar e BottomSheet continuam montados por cima nos dois estados.

## 2. Provider LM Studio (Qwen de base)

**Backend**
- Descobrir o id real do Qwen via `GET http://localhost:1234/v1/models` (primeiro passo da implementação; usuário confirmou que já está instalado).
- `Objects/Enums.cs`: `ChatProviderKind.LmStudio`.
- `Auth/Options.cs`: propriedade `LmStudio` + braço no `For()`.
- **Refactor**: extrair um base `OpenAiCompatibleProvider` (baseUrl, api key opcional, prefixo de erro, `stream_options: { include_usage: true }` p/ usage no streaming) da `OpenRouterProvider` atual; `OpenRouterProvider` herda e só adiciona headers `HTTP-Referer`/`X-Title`; **`LmStudioProvider`** herda sem auth, com timeout de 10 min (inferência local). Role `system` já mapeado nos providers.
- `ProviderFactory` + `Program.cs`: registro do named HttpClient + singleton.
- `appsettings.json`: `"LmStudio": { "BaseUrl": "http://localhost:1234/v1", "DefaultModel": "<qwen-id>" }`.
- `AnonymousChatController`: guest passa a aceitar providers **locais** (Ollama **e** LmStudio); 403 continua para OpenRouter. Fallback de modelo vazio usa `For(req.Provider)` (hoje fixa Ollama).

**Mobile**
- `api/types.ts` + `api/endpoints.ts`: `ChatProviderKind` ganha `'LmStudio'` (provider de `CreateConversationInput`/`listModels` alarga o tipo).
- **Seletor de modelo minimalista** na home: chip acima do composer mostrando o modelo atual (ex.: "gemma4:e2b"); toque abre BottomSheet listando os dois providers locais e seus modelos (`/api/models`); a escolha persiste (novo `stores/modelPrefsStore.ts`, zustand persist AsyncStorage `cortex.modelPrefs.v1`) e vira o default de novas conversas, com fallback para `isDefault` do servidor → primeiro da lista. É o que torna o LM Studio usável no app.

## 3. Instruções básicas de sistema (idioma do celular etc.)

**Backend**
- Novo `ChatOptions` (`Chat:SystemPromptTemplate` no appsettings, opcional) com template padrão embutido contendo placeholder `{language}` — ex.: identidade ("You are Cortex…"), "responda em {language} a menos que o usuário escreva em outro idioma", seja direto/conciso, use Markdown quando ajudar, admita quando não souber.
- Helper `ChatInstructions.Build(locale)`: converte "pt-BR" → "Portuguese (Brazil)" via `CultureInfo` (com fallback se culture inválida) e injeta no template.
- `ChatRequest` e `AnonymousChatRequest` ganham `string? Locale = null`; `ChatService.StreamTurnAsync` recebe o locale e insere `ChatMessagePayload(System, …)` no **início** do histórico (depois do TakeLast(50), para nunca ser truncado); `AnonymousChatController` faz o mesmo. Nenhuma mudança de schema nos providers (system já flui como role).

**Mobile**
- `npx expo install expo-localization`; `sse.ts`: `locale?` em `StartStreamOptions`/`StartAnonymousStreamOptions` (enviado no body); `useChatSession.send` passa `getLocales()[0]?.languageTag` (ex.: "pt-BR").

## Ordem e verificação
1. curl `/v1/models` do LM Studio → fixar `DefaultModel` do Qwen. 2. Backend (enum → options → providers → factory → controllers/ChatService → instruções) → `dotnet build`. 3. Mobile (nav → LM Studio → picker → locale) → `tsc --noEmit`. 4. E2E com API rodando: `/api/models?provider=lmstudio` marcando `isDefault`; POST anônimo em LmStudio com `locale: "pt-BR"` → tokens em português + usage + done.

Sem migrations (enum novo em coluna string). DTOs mudam de forma aditiva (campos opcionais) — `api/types.ts` sincronizado em tandem.